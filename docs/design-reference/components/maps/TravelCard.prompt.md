Compact travel row: destination, distance, drive time, optional "leave in X min", and an Open in Maps action. Used on the pro dashboard and mobile-service booking cards.

```jsx
<TravelCard destination="Al Yasmin District" distanceKm={18} minutes={22} leaveIn={12} onNavigate={openMaps} />
```
