Mobile-professional service radius control (10/20/30 km / Custom) for Business Settings. Enforces that clients outside the radius can't book a mobile appointment.

```jsx
<ServiceAreaSelector value={radius} onChange={setRadius} fee="SAR 30" />
```
