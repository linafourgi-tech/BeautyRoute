Static styled map motif with a location pin — for card/preview contexts (booking summary, address confirm). For an interactive pan/zoom map use Leaflet on a plain-HTML screen, never this. `route` draws a dashed path for schedule previews.

```jsx
<MapCard label="Al Yasmin District" distanceKm={18} route />
```
