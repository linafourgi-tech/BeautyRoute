Underline tab bar — gold underline on active tab, used to switch dashboard sections (Overview / Services / Reviews).

```jsx
<Tabs tabs={["Overview","Services","Reviews"]} active={tab} onChange={setTab} />
```
