Saved-address row for the client's booking flow and profile. Selectable with a gold focus ring; `primary` flags the default.

```jsx
<AddressCard label="Home" address="Al Yasmin District, Riyadh" primary selected onSelect={pick} />
```
