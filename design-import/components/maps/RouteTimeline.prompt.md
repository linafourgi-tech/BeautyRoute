Vertical schedule with drive segments between stops — the pro's "Today's Route". `legs[i]` is the drive from stop i to i+1.

```jsx
<RouteTimeline
  stops={[{time:"10:00",title:"Haircut",place:"Al Olaya"},{time:"12:00",title:"Balayage",place:"Al Malqa"}]}
  legs={[{minutes:11}]} />
```
