Switches the whole client experience between Women's Beauty and Men's Grooming — updates home feed, search, categories, inspiration, AI recs, featured professionals. `cards` for onboarding ("Which experience are you looking for?"); `segmented` for the home header / settings.

```jsx
<ExperienceSwitcher value={exp} onChange={setExp} variant="cards" />
```
