Client-side inspiration image (Pinterest-like masonry tile) — save heart in the corner, style tag overlay. Image is a placeholder until real inspiration photography is supplied.

```jsx
<GalleryCard tag="Balayage" title="Sun-kissed waves" saved={false} onSave={toggleSave} />
```
