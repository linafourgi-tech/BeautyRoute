Save a professional to Favorites — gold heart when saved. `icon` floats over a portfolio/gallery card; `pill` sits on a profile header.

```jsx
<FavoriteButton saved={fav} onToggle={toggle} variant="pill" />
```
