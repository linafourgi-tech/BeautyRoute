Single-line text field with label, hint/error text and an optional leading icon; gold focus ring, no heavy border.

```jsx
<Input label="Email" placeholder="you@example.com" error={emailError} />
```
