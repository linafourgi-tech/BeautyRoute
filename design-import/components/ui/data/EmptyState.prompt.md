Calm, wordmark-style empty state — no illustration, no emoji, just Fraunces headline + muted body + one action.

```jsx
<EmptyState title="Nothing uploaded yet" description="Add your first piece of work to start building trust with clients." action={<Button variant="gold">Upload work</Button>} />
```
