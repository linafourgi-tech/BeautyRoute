Minimal hairline-row table — no zebra striping, no boxed cells — for bookings lists, transaction history.

```jsx
<Table columns={["Client","Service","Date","Status"]} rows={[["Sara","Balayage","Jul 24",<Badge tone="success">Confirmed</Badge>]]} />
```
